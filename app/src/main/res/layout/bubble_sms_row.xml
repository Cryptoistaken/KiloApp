<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/sms_row"
    android:layout_width="match_parent"
    android:layout_height="44dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="8dp"
    android:paddingEnd="8dp"
    android:background="?android:attr/selectableItemBackground">

    <TextView
        android:id="@+id/sms_row_flag"
        android:layout_width="20dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/sms_row_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="8dp"
        android:textSize="11sp"
        android:textStyle="bold"
        android:textColor="#18181B"
        android:maxLines="1"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/sms_row_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:textSize="11sp"
        android:textStyle="bold"
        android:textColor="#71717A"
        android:maxLines="1"
        android:visibility="gone" />

    <ProgressBar
        android:id="@+id/sms_row_spin"
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:layout_marginStart="6dp"
        android:indeterminate="true"
        android:indeterminateTint="#0C0C14"
        android:visibility="gone" />

    <TextView
        android:id="@+id/sms_row_code"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="#1C9C7C"
        android:letterSpacing="0.06"
        android:maxLines="1"
        android:visibility="gone" />
</LinearLayout>
